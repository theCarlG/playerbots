#pragma once
// Redirect — real definition lives in cpp_wrapper/playerbotDefs.h.
// Uses the path relative to the cpp_wrapper/ include directory which
// is already on the include path.
#include "../cpp_wrapper/playerbotDefs.h"
