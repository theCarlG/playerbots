# Minimal config fixture for Phase D parser tests.
# This file deliberately contains only the [AiPlayerbotConf] section header
# and nothing else, so the parser must fall back on defaults for every field.
# If any field here stops having its default in the C++ port, that default
# must be updated in `BotConfig::from_raw` and matched in `minimal_parses_to_defaults`.

[AiPlayerbotConf]
ConfVersion=2020072501
